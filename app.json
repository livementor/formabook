{
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/ruby"
    }
  ],
  "scripts": {
    "postdeploy": "rails db:create && rails db:migrate && rails:db:seed"
  },
  "environments": {
    "test": {
      "scripts": {
        "test": "bundle exec rspec spec/"
      }
    }
  }
}
